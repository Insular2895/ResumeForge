# ResumeForge DOCX Templates

Target template slots for the MVP:

```text
cv_classic.docx
cv_modern.docx
lm_standard.docx
```

The current MVP renderer can regenerate stable DOCX files directly from the
saved JSON session. These files are placeholders for the next template pass,
where the same JSON keys can be mapped to curated Word templates.
